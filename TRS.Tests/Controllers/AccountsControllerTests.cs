using MediatR;
using Microsoft.AspNetCore.Mvc;
using NSubstitute;

namespace TRS.Tests.Controllers;

public class AccountsControllerTests
{
    /*private readonly AccountsController _controller;

    [Fact]
    public async Task CreateAccountAsync_WhenCalled_ReturnsOkResult()
    {
        // Arrange
        var model = new CreateAccountModel
        {
            Username = "test",
            Email = "test",
            Address = "test",
            FirstName = "test",
            LastName = "test",
            PhoneNumber = "test"
        };

        // Act
        var controller = await 

        // Assert
        Assert.IsType<OkObjectResult>(result);
        Assert.NotNull(result);
    }*/

}
